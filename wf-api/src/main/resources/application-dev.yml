server:
  port: 8080
  servlet:
    context-path: /wf-api

spring:
  datasource:
    username:
    password:
    url:
  redis:
    database: 1
    host:
    port:
    password:
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 0
    timeout: 6000ms

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    com.hy: DEBUG